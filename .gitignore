node_modules/
.env
.dev-mongo/
prof/
dev-smoke.png

# Android app (android/) — build output and machine-local config; never the
# release keystore, which must not be committed at all (see android/README.md).
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/*.keystore
android/**/*.keystore
dev/.scratch/

# render-check leaves a composited frame behind so a human can look at it;
# it is a run artefact, not a fixture.
dev/_render-shot.png
# И второй — кадр НА КРАЮ карты: по снимку из середины этажа, где тайлы во
# весь экран, ничего не скажешь о том, что лежит за последним тайлом.
dev/_edge-shot.png
dev/_load-shot.png
dev/_splash-shot.png
